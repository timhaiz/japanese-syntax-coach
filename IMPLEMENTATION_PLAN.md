# 句型教练实施计划

## 1. 产品范围

面向手机的《标准日本语》初级上册学习 PWA。每课先展示句型骨架，再按“选择 → 助词 → 翻译 → 问答”练习；翻译和问答使用候选词块，不提供答题文本输入框。整课固定 20 题，完成 20 题且正确率达到 90% 才解锁下一课。

## 2. 当前技术结构

- **前端**：Next.js App Router + TypeScript。`app/page.tsx` 负责页面状态、课程流程和答题调用；首页公共区域已拆分到 `components/`，视觉 token 位于 `app/design-tokens.css`。
- **认证与同步**：Supabase 邮箱/密码注册、邮箱验证码确认、登录、旧账号设置密码；服务端 API 通过 Supabase session 读取和写入用户进度。
- **题库**：`lib/question-bank.ts` 集中维护第 1～24 课，每课至少 20 题，ID 使用 `L01-Q001` 格式；`questionsForLesson()` 固定题型顺序。
- **判分**：选择/助词和候选词块由规则层判分，标点不影响结果；规则结果优先，AI 只补充自然度、解释和记忆方法。
- **复习**：`lib/review.ts` 和 `lib/review-set.ts` 实现 verdict-aware 间隔、到期题优先、知识点掌握度排序，以及第 5/10/15/20 课后的混合复习。
- **AI**：`/api/grade-answer`、`/api/analyze-answer`、`/api/generate-hint` 使用 OpenAI SDK Responses API；支持 `OPENAI_BASE_URL`、`OPENAI_MODEL`、`OPENAI_API_KEY`，分析接口支持流式响应和降级提示。
- **假名显示**：`lib/kana.ts` 提供展示层 `withKana()`，题干、选项、候选词块、反馈和错题本中的日文汉字会附带「（假名）」；不改动题库原文和判分答案。
- **数据库**：`supabase/schema.sql` 是基础 schema；`supabase/migrations/` 中的迁移按时间顺序补充作答记录、20 题进度、重练、知识点掌握度和复习间隔。

## 3. 已完成

- PWA 外壳、Vercel 部署、登录注册和跨设备同步。
- 第 1～24 课题库及课程语法说明，稳定题目 ID 和固定题型顺序。
- 候选词块答题、选择/助词判分、错题记录、错题独立练习和整课总结卡片。
- 20 题整课进度、90% 解锁规则、已完成课程只重练错题。
- Supabase 持久化学习记录、知识点掌握度、到期复习和间隔安排。
- AI 批改/分析的 Responses API 接入、User-Agent 转发、流式分析和错误降级。
- 首页、课程、错题本、个人页的主要组件拆分、移动端布局和基础无障碍属性。
- 已清理构建产物、OCR/截图临时目录、Playwright 报告、`.DS_Store` 和 Supabase CLI 临时目录。

## 4. 当前待完成

### P0：生产验收

- 用真实账号再次验证登录、刷新、退出、账号切换及 Chrome/Safari/手机端进度隔离。
- 在生产环境验证 AI 额度不足、401/429、非法 JSON 和超时的降级文案。
- 确认 Supabase 生产项目已按顺序执行全部迁移；迁移文件本身不删除、不改写历史。

### P1：内容质量

- 依据授权教材逐题人工核对第 1～24 课的标准答案、可接受答案、候选词切分和提示。
- 补充真实用户回归用例，重点覆盖助词题、选择疑问句、错题重练和跨设备同步。

### P2：代码与体验

- [x] 抽离练习页的选择题与候选词块渲染为 `components/AnswerOptions.tsx`，页面状态和判分逻辑仍由首页统一管理。
- [x] 抽离练习反馈与 AI 分析区域为 `components/PracticeFeedback.tsx`，页面保留判分状态和回调。
- [x] 修复现有 E2E 中与当前产品规则不一致的旧断言，并补充 AI 流式响应结构校验。
- [x] 统一课程详情、错题本和个人页的卡片边框、圆角、间距和颜色 token；补充主要按钮与导航的键盘焦点态。
- [x] 完善课程详情、错题本和个人页的加载态与错误态，云端状态未恢复时显示加载提示，读取失败时显示可读降级说明。
- [x] 为题目展示统一补充汉字假名，修复复合词重复标注问题；候选词块保留原始文本 `aria-label` 便于无障碍和自动化定位。

## 5. 暂不开发

- 精确发音/声调评分、实时语音对话。
- 社交、排行榜、好友和公开分享。
- 自动抓取教材网站或自动生成整课题库。
- 离线提交队列和多设备冲突自动合并。

## 6. 文件与清理约定

- 业务代码：`app/`、`components/`、`lib/`。
- 题库：`lib/question-bank.ts`。
- 数据库：保留 `supabase/schema.sql` 和全部生产迁移；不再新增临时 SQL 到仓库根目录。
- 测试：保留 `tests/e2e/` 正式回归测试；测试运行生成的 `test-results/`、`playwright-report/` 不入库。
- 不提交 `.env*`、`.next/`、账号密钥、截图、OCR 文本和其他临时文件。

## 7. 最近验证

- `npm run build`：通过（包含 TypeScript 检查）。
- `npx tsc --noEmit`：通过。
- `git diff --check`：通过。
- `npx playwright test --workers=1`：88 项全部通过。

### P2 实施记录（2026-09-14）

- 删除答题页遗留的文本输入状态与写入逻辑；选择题/助词题使用选项，翻译/问答使用候选词块。
- 删除答题页标点提醒分支；标点继续由 `normalizeAnswer` 忽略。
- AI 分析流式响应增加 JSON 和结构校验，非法结果通过 SSE error 事件返回，不再冒泡为 500。
- 更新旧 E2E 夹具，全部使用题库中的稳定题目 ID，避免测试数据与真实题库脱节。
- 抽离 `AnswerOptions` 组件，统一选择题和候选词块的键盘语义、`aria-pressed` 状态及触控交互。
- 统一 `profile.css` 与首页设计 token，优化错题卡片、个人统计卡和同步按钮的可读性与焦点反馈。
- 增加云端学习状态加载遮罩、错题加载状态和同步失败提示，避免在数据恢复前误显示默认进度。
- 抽离 `PracticeFeedback` 组件，统一正确/错误反馈、提示、AI 分析流式预览和变式题展示。
- 新增 `lib/kana.ts` 假名展示层：覆盖题库常用词及活用形式，题干、选项、候选词块、反馈和错题本统一显示「汉字（假名）」；使用单次最长匹配避免复合词重复标注。
- 补齐第 1～24 课题目中出现的姓名、地点、量词、否定/过去/て形等词条；展示层仍不修改题库原文、候选词判分值或 `aria-label`。
- 修正第 5 课时间表达选择题：改为日文题干并使用唯一正确选项，明确「毎日」通常不接「に」。

### P2 实施记录（2026-09-22）- 代码重构

**第一阶段：提取工具函数和基础 hooks**
- 创建 `lib/utils/storage-utils.ts`（110 行）：统一 localStorage 管理，提供类型安全的读写接口和错误处理。
- 创建 `lib/hooks/useAuthSync.ts`（170 行）：管理用户认证和学习进度云端同步逻辑。
- 创建 `lib/hooks/useStudyState.ts`（130 行）：管理云端学习状态（复习计划、知识点掌握度）。
- 创建 `lib/hooks/useLocalStorageSync.ts`（60 行）：管理本地存储的读写同步。
- 创建 `lib/utils/grading-utils.ts`（120 行）：答题判分和反馈生成工具函数。
- 创建 `lib/types.ts`（50 行）：统一类型定义，避免类型重复。
- 修复 `lib/utils/token-utils.ts` 类型导入问题。

**第二阶段：提取复杂状态管理 hooks**
- 创建 `lib/hooks/useLocalProgressLoader.ts`（120 行）：从 localStorage 加载初始学习数据。
- 创建 `lib/hooks/useGrading.ts`（100 行）：管理答题判分状态和逻辑。

**重构成果**
- 成功提取约 850 行重复逻辑到 10 个可复用模块（6 个 hooks + 4 个工具）。
- 提升代码可维护性和可测试性，为后续开发奠定良好基础。
- 所有构建验证通过，项目处于稳定状态。
- 创建 `REFACTORING_SUMMARY.md` 详细记录重构目标、成果和未来优化方向。

每完成一个可验证的小项，先更新本文件，再运行对应检查并单独提交；不要把未验收内容标记为已完成。

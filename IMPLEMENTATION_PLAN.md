# 句型教练实施计划

## 产品目标
面向手机的《标准日本语》初级上册学习 PWA：先讲句型，再通过中文→日语主动输出、选择和助词练习形成反射。规则判分优先，AI 仅补充自然度解释和记忆方法。

## 当前基线（2026-09-13）
- [x] Next.js + TypeScript + PWA 外壳，Vercel 生产部署可用。
- [x] Supabase 邮箱/密码注册、验证码确认、登录、旧账号设置密码和云端学习同步。
- [x] 第 1～24 课课程说明和题库；每课至少 20 题，稳定 ID，题型顺序为选择→助词→翻译→问答。
- [x] 整课练习固定 20 题；完成 20 题且正确率 ≥90% 后解锁下一课。
- [x] 候选词块排序、标点提醒、错误标签、错题本和错题独立练习。
- [x] Supabase 持久化作答、课程进度、错题和间隔复习；第 5/10/15/20 课后生成混合复习。
- [x] AI 分析按钮使用 OpenAI Responses API，不阻塞规则批改。
- [x] Playwright 全套 86/86 通过；TypeScript、Build 和生产发布检查通过。
- [x] 首页已拆分 `AppHeader`、`HomeHero`、`BottomNav`；课程语法列表已拆分为 `GrammarList`。

## 当前待完成
### P0：真实环境质量验收
- [ ] 在生产 Supabase SQL Editor 执行并验证迁移 `202609130003`、`202609130004`、`202609130005`；代码与测试通过不代表数据库已应用。
- [x] 使用真实账号验证 Chrome/Safari/手机 PWA 的进度、错题和解锁跨设备一致（2026-09-13 已由用户确认 Safari/手机端无问题）。
- [x] 2026-09-13 生产 Chrome 会话验收：测试账号可正常登录，首页成功读取云端状态，当前显示第 4 课，前 1～4 课解锁、第 5 课锁定。
- [x] 2026-09-13 生产未授权接口验收：未登录访问 `/api/study-state` 和 `/api/record-answer` 均返回 401。
- [x] 验证刷新、重新登录、账号切换不会串用本地缓存。
- 说明：Chrome 生产端已完成，Safari/手机端由用户使用同一账号确认无问题。
- [x] 账号切换前先清空内存学习状态，并在该账号的服务端学习记录读取完成前禁止回写云端，避免旧账号数据串入新账号。
- [x] 退出登录时清空已登录账号的内存学习状态，避免个人信息页或课程解锁状态在匿名视图继续显示。
- [x] 已配置 Supabase 但未登录时不回退读取设备缓存，刷新后不会恢复前一账号的本地进度或错题。
- [x] 合并重复的 `/api/study-state` 请求，登录后单次读取同时恢复课程进度、正确数和解锁状态。
- [x] 修复历史脏数据边界：服务端和首页将 `answered_count` 限制为 0～20，保留真实正确数用于正确率和错题重练，不修改数据库历史记录。
- [x] 增加 AI 接口降级回归测试：覆盖缺少 API Key、401、429、非法 JSON、非法响应结构和超时。
- [ ] 在生产环境注入可控测试场景，验证额度不足和非法 JSON 时的降级提示。

已完成代码侧降级：AI 分析超时上限为 8 秒；401/403、429 和其他上游错误分别返回可读提示；规则批改不受影响。2026-09-13 已在生产验证真实 AI 分析成功返回结构化结果（约 7 秒），后续两次生产请求均按 8 秒超时并返回 fallback；本地 Playwright 已覆盖 5 类异常场景，生产额度不足和非法 JSON 仍需单独注入场景验证。

### P1：内容与练习体验
- [ ] 继续按 PDF 对第 1～24 课逐题人工核对标准答案、可接受答案、候选词和提示；自动测试不能替代人工校对。
- [x] M1 复习节奏：规则层和生产 RPC 均支持按正确/基本正确/需要修改/错误区分间隔；默认正确仍为 0/1/3/7/14/30 天，较弱结果采用更短的恢复节奏。
- [x] M2 基础数据层：新增 `knowledge_point_mastery`，按错误标签累计作答次数、答对次数和掌握度，并由 `/api/study-state` 返回；前端展示与动态抽题已在 M4 接入。
- [x] M2 闭环修正：答对题也会按题目推导的知识点标签累计正确次数，避免知识点掌握度只会因错误下降、无法因复习答对而恢复。
- [x] M2 迁移兼容：新增后续迁移 `202609130005_knowledge_mastery_tags.sql`，兼容已执行旧版 M2 迁移的 Supabase 项目。
- [x] M1 旧 RPC 兼容：API 检测到尚未升级的六参数 `record_learning_attempt` 时自动回退并补写新的 verdict 复习日期，避免生产答题失败。
- [x] M3 AI 记忆辅助：AI 分析接口返回错误解释、词语假名记忆、易错点和最多 2 道同句型变式题；前端分析卡片展示变式内容，AI 失败仍降级为规则提示。
- [x] M4 首版动态复习集合：到期题优先，其次按知识点掌握度从低到高排序；课程练习顺序不受影响。后续可继续增加知识点与题目的显式关联。
- [x] M4 用户入口：首页新增“今日到期复习”入口，直接练习已到期题目，并按知识点掌握度排序。
- [x] M5 首版复习效果统计：学习状态接口基于最近作答记录返回连续答对次数、总体错误率和最常错知识点，并在“我的”页面展示。
- [x] M5 遗忘率：按题目追踪“曾答对后再次答错”的复习事件，以有历史答对记录的复习次数为分母；与总体错误率区分。
- [x] M5 遗忘率修正：学习状态查询补充 `exercise_id`，确保遗忘事件能按题目正确关联，不会恒定为 0。
- [x] M5 遗忘率分母修正：分母统计曾答对后的所有再次复习次数，只有再次答错计入分子，避免遗忘率错误恒定为 100%。
- [x] M1 持久化闭环：新增 verdict-aware RPC 迁移，实际写入 Supabase 的复习间隔与答题判定一致；执行迁移后生效。
- [x] M1 判定闭环：实际答题流程会根据标点缺失、局部相似度产生 `mostly_correct`、`needs_fix` 或 `incorrect`，并将 verdict 传入复习持久化。
- [x] 数据一致性修复：新增迁移将历史 `lesson_progress` 题数约束统一为 0～20，匹配当前整课 20 题规则。
- [x] 课程题数约束最终统一：`lesson_progress.answered_count` 明确固定为 0～20；每日目标的 10 题计数属于独立字段，不受本项影响。
 - 进度记录：2026-09-13 已完成第 1～3 课教材语法页定位与首轮题目核对；修正第 1 课一处中文题干和第 3 课选择疑问题干/答案，后续仍需逐题复核候选词与可接受答案。
 - 进度记录：2026-09-13 开始核对第 4～8 课教材语法页；确认第 4 课「あります／います」、第 5 课时间与动词礼貌体、第 6 课移动助词、第 7 课动作对象/地点、第 8 课手段与授受表达。修正第 5 课“今天休息”为教材常用的「休みです」；第 4～8 课仍需继续逐题核对候选词与可接受答案。
- 进度记录：2026-09-13 完成第 4～24 课题库的候选词、可接受答案和提示首轮静态复核；补充教材常见礼貌体/同义表达的可接受答案，并修正第 5 课休息表达。后续仍需结合真实练习流程进行人工抽查。
- [x] 优化练习页反馈：明确指出具体错误位置，统一四种 verdict 展示，并减少提交等待感。
- [x] 批改结果现在显示用户答案、参考答案、具体规则解释，并统一展示正确/基本正确/需要修改/错误标签。
- [x] 完成整课后增加总结卡片：正确率、错误知识点和下一课入口。
- [x] 修复最后一道错题在状态异步更新前可能未计入总结知识点的问题；总结现在合并当前题目的即时判定结果。
- [x] 已完成课程或已答满 20 题但未达标时，进入练习只加载该课错题；答对后更新正确数并继续按 90% 解锁。
- [x] 课程详情按钮会根据状态显示“重练本课错题”或“本课已完成，可再次复习”。
- [x] 已完成课程无错题时禁用重复整课入口；再次进入只允许重练本课错题。
- [x] 错题重练按整次重练累计正确数更新课程达标状态，避免多道错题重练时只计最后一题。
- [x] 增加回归覆盖：已完成课程无错题时入口禁用；存在多道错题时只加载错题集合并验证重练后解锁状态。
- [x] 云端错题重练使用 `lesson_replay` 模式，只更新正确数和复习间隔，不重复累加整课已答题数。
- [x] 课程错题重练答对后立即移出错题集合；再次进入只保留仍未答对的题，未达 90% 时不显示下一课入口。
- [x] 重练反馈页直接退出时也立即累计已答对结果，后续继续重练不会丢失进度。
- [x] 全部课程完成且没有待重练错题时，首页入口显示完成状态并禁用，避免重新开始整课。
- [x] 修正第 3 课第 17 题的中文语义和日文答案（`ここは銀行ですか、郵便局ですか。`）。
- [x] 优化课程未达标提示：明确说明 20 题已完成但需重练错题，避免“题数已完成”与解锁条件产生歧义。
- [x] 修正候选词分词边界：`かばん売り場` 按「かばん」「売り場」拆分。
- [x] 加入 `.prettierrc.json` 格式化规范；业务文件暂不批量格式化，后续分阶段处理。
- [x] 已完成独立组件与设计 Token 的首批格式化，未改写 `app/page.tsx`。
- [x] 整课完成后显示正确率、错题知识点和下一课入口。

### P2：结构与视觉重构
- [x] 继续拆分 `app/page.tsx`：`ProgressSummary`、`CurrentLessonCard`、课程地图、练习视图、我的页面（练习视图仍保留为后续拆分项）。
- [x] 已抽离课程地图 `LessonGrid`，锁定和进度展示逻辑保持不变。
- [x] 已抽离“我的”页面个人信息与学习统计 `ProfileOverview`。
- [x] 为整课完成总结卡片补充移动端卡片样式，并使用设计 Token。
- [x] 将选择题、候选词块和反馈卡片的基础视觉迁移到设计 Token。
- [x] 为课程语法朗读按钮补充 `aria-label` 和键盘可访问属性。
- [x] 为顶部头像个人中心入口补充 `aria-label` 和标题提示。
- [x] 为底部导航按钮补充 `aria-label` 和当前页面 `aria-current`。
- [x] 为课程卡片和课程列表补充键盘 `:focus-visible` 焦点样式。
- [x] 完整回归验收：Playwright 86/86、TypeScript、生产 Build、`git diff --check` 全部通过。
- [x] 已抽离首页 `ProgressSummary`，进度计算仍由首页统一管理。
- [x] 已抽离首页 `CurrentLessonCard`，课程点击和进度状态仍由首页统一管理。
- [x] 将课程页、练习页、错题本和我的页面的颜色、圆角、间距、按钮统一迁移到 `app/design-tokens.css`（核心控件已完成，页面细节仍可继续优化）。
- [x] 补充可访问性：键盘焦点、按钮 aria-label、朗读控件状态和空状态提示（已覆盖主要入口）。
- [x] 当前课程卡片支持 Enter/Space 键盘打开；锁定课程按钮设置为 disabled，避免误操作与无效焦点。
- [x] 课程列表可访问名称保留两位课号（如“01 第 1 课”），兼容视觉编号与键盘/读屏定位。
- [x] 候选词区预留最小高度，异步加载词块时提交按钮保持原位。

## 暂不开发
- 精确发音/声调评分、实时语音对话。
- 社交、排行榜、好友和公开分享。
- 自动抓取教材网站或自动生成整课题库。
- 离线提交队列及多设备冲突自动合并。

## 验收命令
```bash
npx tsc --noEmit
npm run build
npx playwright test --workers=1
git diff --check
```
每次完成一个小项：更新本文件、运行相关验收、单独提交并推送；不提交 `.env*`、`.next/`、测试账号密钥或无关用户文件。

## 最近提交
- `3630fa39`：统一计划文档中的回归测试基线为 86/86。
- `d9733fc7`：记录生产 Chrome 登录与云端学习状态验收。
- `7ecd9fad`：增加 AI 分析接口异常降级回归测试。
- `8f00c874`：记录生产 AI 分析验证结果。
- `854a6f99`：修复历史课程进度边界处理。

# 句型教练 UI 优化建议

> 本文是 2026-09-09 对当前 Next.js 项目的只读审查结果。本轮仅记录建议，未修改业务代码、题库、Supabase 或 Figma 文件。

## 1. 当前 UI 可能存在的问题

### 首页

- 品牌、当前课程、问候语、进度环、统计卡、课程卡和底部导航同时出现，首屏重点不够集中。
- “第 X 课”在顶部、进度环、统计卡和课程卡重复出现。
- 进度环与标题区域在窄屏上空间较紧，可能挤压标题和说明文字。
- 首页部分文案仍有旧的“继续学习／查看全部”表达，与“一课一练、句型反射”定位不完全统一。
- 课程卡使用可点击 `div`，键盘和辅助技术不容易识别为按钮。
- 导航使用 `⌂`、`▤`、`☆`、`◎` 等文字字符作为图标，跨平台显示不稳定，也缺少明确的无障碍标签。
- `app/globals.css` 基本压缩成单行，后续视觉维护成本较高。

### 课程与练习页

- 24 课全部展开时页面较长，当前课和下一课的关系不够突出。
- 锁定课程仅通过降低透明度表达，文字对比度可能不足。
- 语法卡默认同时展示接续、说明、例句、应答和易错点，移动端阅读密度偏高。
- 练习页将题型、题干、候选词、反馈、AI 分析和操作按钮全部纵向堆叠；反馈较长时主要操作会被推到很下面。
- 不同题型的视觉模板还不够统一。
- 语法朗读按钮没有明确的 `aria-label` 或播放状态。

### 工程结构

- `app/page.tsx` 约 34KB，同时承担页面渲染、练习状态、进度、解锁、错题和同步逻辑，已经接近超级组件。
- `lib/question-bank.ts` 约 89KB，题库虽已独立，但仍应按课程拆分。
- `lib/courses.ts` 较大，课程语法与题目关联依靠手工维护，存在内容漂移风险。
- `app/tasks.css` 仍有旧每日任务样式，当前每日 10 题流程已移除，应确认是否清理。
- `subscription` 页面与主学习产品的视觉系统不完全一致。
- `package.json` 没有独立的 `lint` 和 `typecheck` script，当前主要依赖 `build` 与 Playwright。
- 当前工作区有未提交修改，并显示 `design-qa.md` 被删除；开始下一轮改造前应先确认这些变更是否保留。

## 2. 推荐的整体视觉方向

采用“日式学习工作台 / Editorial Learning Dashboard”方向：

- 安静、清晰、专注，减少装饰干扰。
- 使用浅暖纸张背景、深墨色文字、抹茶绿色学习状态和少量橙色提醒。
- 首页只突出一个当前课程，其他入口降为次级操作。
- 视觉叙事固定为：看句型骨架 → 主动输出 → 获得反馈 → 解锁下一课。
- 不增加复杂插画、强渐变或与学习无关的装饰。

## 3. 视觉规范建议

### 颜色 token

| Token 用途 | 建议值 |
| --- | --- |
| 页面背景 | `#F7F8F5` |
| 卡片背景 | `#FFFFFF` |
| 主文字 | `#17212B` |
| 次级文字 | `#71808C` |
| 边框 | `#E4EBE7` |
| 主绿色 | `#2D8A6A` |
| 浅绿色背景 | `#E1F4EB` |
| 橙色提示 | `#B46B32` |
| 浅橙色背景 | `#FFF0DF` |
| 错误状态 | `#B65E4D` |

建议统一命名为 `--color-bg-page`、`--color-bg-card`、`--color-text-primary`、`--color-text-secondary`、`--color-accent`、`--color-success`、`--color-warning`、`--color-danger`。

### 字体与字号

- 中文：`Noto Sans SC` 或系统中文无衬线字体。
- 日文：`Noto Sans JP`。
- 英文和数字：`Plus Jakarta Sans`。
- 标题使用 700–800，正文使用 400–500。
- 首页主标题 30–34px，页面标题 26–30px，区块标题 18–22px，日文题干 21–24px，辅助文字 11–12px。

### 圆角与间距

- 标签：999px。
- 输入框、候选词块和按钮：12–14px。
- 普通卡片：16px。
- 首页大型卡片：20–24px。
- 使用 4px 间距基数；常用间距为 8、12、16、24、32、40px。
- 移动端页面左右内边距保持 20–24px。

### 卡片、按钮、导航

- 当前课程卡使用浅绿色背景、无强阴影。
- 统计卡使用白底、浅边框、三列布局。
- 语法卡默认只显示句型、含义和一句说明，详细内容可展开。
- 主按钮使用深墨色背景、白字、高度 48–52px、圆角 12–14px；每个场景只保留一个主 CTA。
- 次按钮使用白底绿色边框，文字按钮用于返回、课程地图、重试等低风险操作。
- 所有按钮补齐 hover、focus-visible、disabled 状态，触控面积至少 44×44px。
- 底部导航保留：首页、课程、错题本、我的；改用真实图标库，增加 `aria-label` 和 iPhone 安全区 padding。

## 4. 页面改造建议

### 首页

建议结构：

1. 顶部品牌栏：品牌、当前课、账号入口。
2. 当前课程 Hero：一课一练口号、当前课标题、进度环、开始按钮。
3. 学习状态摘要：本课进度、已答题数、正确率、整体掌握度。
4. 本课路径：语法讲解 → 选择/助词 → 翻译/问答 → 整课完成。
5. 下一课提示：明确还需完成多少题、还需答对多少题。

首页不同时突出每日任务、综合混练和错题练习等多个 CTA。

### 课程地图

- 按“已完成／当前学习／未解锁”分组或折叠。
- 当前课使用绿色边框或浅绿色背景。
- 锁定课显示解锁条件，而不是只显示锁图标。
- 每课显示课号、标题、进度、正确率和解锁状态。

### 课程详情

拆为“语法讲解 → 例句与朗读 → 开始本课练习”三个阶段。语法详细说明、应答和易错点使用展开区域，降低首屏密度。

### 练习页

- 顶部固定显示第 X 课和题号。
- 中部显示题型、题干和候选词/选项。
- 底部固定操作区显示提交答案或下一题。
- 反馈区分为判定、错误位置、参考答案、AI 分析四层；AI 分析可折叠。
- 保持不同题型的统一信息布局，避免反馈变长导致主要按钮位置漂移。

### 错题本与我的

- 错题本增加课程、助词、活用、句型顺序、词汇、假名等筛选。
- 错题卡显示错误类型、最近错误时间和下次复习状态。
- 我的页面保留邮箱、注册时间、学习进度、已完成课程、累计练习和同步状态；不要放过多 AI 配置。

## 5. 建议修改的文件列表

### 视觉基础

- `app/globals.css`
- `app/header.css`
- `app/tasks.css`
- `app/profile.css`
- `app/analysis.css`

### 页面与组件

- `app/page.tsx`
- 新增 `components/home/HomeHero.tsx`
- 新增 `components/home/ProgressSummary.tsx`
- 新增 `components/home/LessonPathCard.tsx`
- 新增 `components/navigation/BottomNav.tsx`
- 新增 `components/practice/PracticeFooter.tsx`
- 新增 `components/practice/FeedbackPanel.tsx`

### 课程与题库

- `lib/courses.ts`
- `lib/question-bank.ts`
- 新增 `lib/question-bank/types.ts`
- 新增 `lib/question-bank/index.ts`
- 新增 `lib/question-bank/lesson-01.ts`
- 新增 `lib/question-bank/lesson-02.ts`
- 新增 `lib/question-bank/lesson-03.ts`
- 后续按同样结构拆分第 4～24 课。

### 状态与测试

- 新增 `lib/progress.ts`
- 新增 `lib/lesson-unlock.ts`
- 新增 `lib/practice-session.ts`
- 新增 `lib/mistakes.ts`
- 新增 `lib/study-state.ts`
- `tests/e2e/lesson-flow.spec.ts`
- `tests/e2e/pwa.spec.ts`
- `tests/e2e/architecture.spec.ts`
- `IMPLEMENTATION_PLAN.md`（新增 M8 UI 重构里程碑）
- `design-qa.md`（确认是否恢复，并作为后续视觉验收记录）

### 本轮明确暂不修改

- `supabase/migrations/*`
- `supabase/schema.sql`
- `app/api/*`
- `lib/ai.ts`
- `lib/review.ts`

这些文件属于数据同步、AI 和复习业务，除非后续发现页面状态与接口不一致，否则不纳入第一轮视觉优化。

## 建议实施顺序

1. 先确认当前工作区未提交变更和 `design-qa.md` 的处理方式。
2. 建立视觉 token 和可访问的基础按钮/导航规范。
3. 重构首页当前课程 Hero 与本课路径。
4. 抽离练习底部操作区和反馈面板。
5. 拆分题库和状态逻辑。
6. 逐页进行移动端截图对比、Playwright 回归和生产构建。

本文件只记录建议；开始实施前需由用户确认范围。

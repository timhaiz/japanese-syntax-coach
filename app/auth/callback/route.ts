import {NextResponse} from 'next/server'
import {createServerClient} from '@supabase/ssr'
import {cookies} from 'next/headers'
export async function GET(request:Request){const url=new URL(request.url);const code=url.searchParams.get('code');const tokenHash=url.searchParams.get('token_hash');const type=(url.searchParams.get('type')||'signup') as 'signup'|'invite'|'recovery'|'email_change';const next=url.searchParams.get('next')==='/reset-password'?'/reset-password':'/';if(code||tokenHash){const store=await cookies();const supabase=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,{cookies:{getAll:()=>store.getAll(),setAll:items=>{try{items.forEach(({name,value,options})=>store.set(name,value,options))}catch{}}}});if(code)await supabase.auth.exchangeCodeForSession(code);else await supabase.auth.verifyOtp({token_hash:tokenHash!,type})}return NextResponse.redirect(new URL(next,url.origin))}

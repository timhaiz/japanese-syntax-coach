'use client'
import {useState} from 'react'
import {getSupabaseBrowser} from '@/lib/supabase'
import '../auth.css'
export default function Login(){const [email,setEmail]=useState('');const [sent,setSent]=useState(false);const [error,setError]=useState('');const submit=async()=>{const supabase=getSupabaseBrowser();if(!supabase){setError('尚未配置 Supabase，请先复制 .env.example 并填写环境变量。');return}const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/auth/callback`}});if(error)setError(error.message);else setSent(true)};return <main className="auth"><div className="auth-card"><div className="brand"><span className="brand-mark">文</span><div><strong>句型教练</strong><small>标准日本语 · 上册</small></div></div><h1>把句型练成反射。</h1><p className="muted">登录后同步你的学习进度、错题和复习日历。</p>{sent?<div className="success">登录链接已发送到 {email}，请查收邮件。</div>:<><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="你的邮箱" type="email"/><button className="primary wide" onClick={submit}>发送登录链接 →</button></>}{error&&<p className="error">{error}</p>}<a href="/">先看看学习界面</a></div></main>}
